{{ form_start(form) }}
    {{ form_errors(form) }}

    <ul class="list-inline">
        {% include 'BWSkeletonBundle:Form:control-button-list.html.twig' %}
        <li>
            <a class="btn btn-default fas fa-ban" href="{{ path('post') }}" title="Отменить несохраненные изменения"> Закрыть</a>
        </li>
    </ul>

    <div class="form-inline">
        <div class="form-group">{{ form_row(form.published) }}</div>&nbsp;
    </div>

    <div class="form-group">{{ form_row(form.category) }}</div>
    <div class="form-group">{{ form_row(form.heading) }}</div>
    <div class="form-group">{{ form_row(form.shortDescription) }}</div>
    <div class="form-group">{{ form_row(form.description) }}</div>
    <div class="form-group">{{ form_row(form.class) }}</div>
    <div class="form-group">{{ form_row(form.order) }}</div>
    <div class="form-group">{{ form_row(form.created) }}</div>
    <div class="form-group">
        {% if form.image.vars.data and form.image.vars.data.webPath %}
            <img src="{{ asset(form.image.vars.data.webPath)|imagine_filter('medium_thumb') }}"
                 title="{{ form.image.vars.data.title }}"
                 alt="{{ form.image.vars.data.alt }}"
            />
        {% endif %}
        {{ form_row(form.image) }}
    </div>

    <h2><i class="fa fa-sliders"></i> SEO</h2>
    {% if entity.route %}
    <div class="form-group">
        Адрес страницы:
        <a class="fa fa-share-square-o" href="{{ app.request.getUriForPath(entity.route.path) }}" title="Перейти на страницу статьи"></a>
        <code>{{ entity.route.path }}</code>
    </div>
    {% endif %}
    <div class="form-group">
        {{ form_row(form.slug) }}
        <ul class="text-muted">
            <li>Используйте прямой слеш <code>/</code> в начале синонима URL чтобы переопределить стандартное формирование.</li>
            <li>Используйте адрес страницы <code>/{{ home_page_path }}</code> чтобы сделать страницу главной.</li>
        </ul>
    </div>
    <div class="form-group">{{ form_row(form.title) }}</div>
    <div class="form-group">{{ form_row(form.metaDescription) }}</div>

{{ form_end(form) }}
