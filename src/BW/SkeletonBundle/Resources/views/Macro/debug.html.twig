{%- macro template(templateName, blockName = null, positionName = null) -%}
    {%- if(app.debug) -%}
        <!-- {{ (positionName ? positionName : "") ~ templateName ~ (blockName ? " :: block " ~ blockName : "") }} -->
    {%- endif -%}
{%- endmacro -%}
